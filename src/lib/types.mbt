/// `sint32`
pub type SInt Int derive(Default, Eq, Show)

/// `sint64`
pub type SInt64 Int64 derive(Default, Eq, Show)

/// length
pub type Len Int derive(Default, Eq, Show)

/// Enum
pub type Enum UInt derive(Default, Eq, Show)
